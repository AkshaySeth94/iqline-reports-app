Dev stage failed the inline-style density guard.

Dev stage produced frontend files that bypass the declared styling system — each file below has > 5 inline style={{...}} attributes, indicating bare-HTML output with no design system.

  frontend/src/components/GlucoseChart.tsx: 6 inline style={{...}} occurrences (example lines: 32, 42, 43)

The Architect declared a styling system in deployment-doc.md (Tailwind / shadcn-ui / CSS modules / etc.). Replace every `style={{ ... }}` with the equivalent in that system:

  ❌  <button style={{ padding: 8, background: '#3b82f6', color: 'white', borderRadius: 4 }}>
  ✅  <button className="px-2 py-2 bg-blue-500 text-white rounded">     // Tailwind
  ✅  <Button variant="primary" size="sm">                                  // shadcn-ui / Mantine
  ✅  <button className={styles.primaryButton}>                            // CSS Modules

Legitimate dynamic inline styles (\`style={{ width: \`${pct}%\` }}\`) are fine and contribute to the threshold sparingly — but every static color/spacing/border/font value goes in the declared system, not in JSX style props. Re-emit the listed files using the styling system Architect chose.